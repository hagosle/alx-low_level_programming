#include "main.h"
#include <stdio.h>

/**
 * _isupper -checks if the letter is uppercase
 * @c: print int
 * Return: Always 0.
 */
int _isupper(int c)
{
char c;

    c = '0';
    printf("%c: %d\n", c, _isdigit(c));
    c = 'a';
    printf("%c: %d\n", c, _isdigit(c));
    return (0);[D[D[C[C
}[A[A[A[A[A[A[A[D[D       [D[D[Dchar c;[B[B[B[B[B[B[B